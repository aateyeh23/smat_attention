Training source-resolution defect: Python -m prioritized its /opt/smat/deltaai working directory over /opt/transport and /opt/iterate in PYTHONPATH. The frozen bundled content_addr.py was therefore loaded during training, while script-based GPU validation used the modified content_addr.py. Changes implemented only in the overlay router were not exercised; model-wrapper changes could still apply. The unmodified resume trainer also ignored trial21 optimizer_parameters, confirmed by its one-group checkpoint. Raw accuracies and checkpoints are preserved, but intended ablation attributions are unreliable. Trials01–21 must not be treated as verified tests of the advertised full routing implementation. The pure-GDN baseline is unchanged. Source auditing and safe-path launch begin at trial22.

Affected router-only mechanisms include restored neighboring write gradients, symmetric gradients, read exploration noise, joint occupancy balance, periodic neighbors, address-based/concurrent readers, and the delta write-gradient adapter. In address-reader variants, the wrapper froze the categorical reader even though the old router still used it. Trial19 gradient clipping ran in the unique overlaid gated trainer and its norm logs are valid, but the router was still the old one. Probe07 and probe17 normal forward analyses remain evaluations of saved weights because their no-grad hard forward matches for those configurations; they do not validate the advertised training gradients.

The earlier standalone neighbor-write-gradient transport run used the same unsafe Python -m launch pattern (`modal_gdn_transport.py`). Its intended restored router gradient was likewise shadowed by the frozen bundled router. Its raw checkpoint/metrics remain archived, but it is not evidence for the restored-gradient mechanism. The standalone launcher now also sets PYTHONSAFEPATH=1; the successful iterative launcher additionally uses explicit -P and runtime path/hash assertions.
