Frozen bundled modules that shadowed the experimental overlay during trials01–21. Their precedence is reconstructed from the launch command and Python import rules; those runs did not record runtime source audits. Preserve these alongside each trial's uploaded model-wrapper snapshot when reproducing historical forward behavior. In particular, address-reader variants froze the categorical read parameters, but this router continued to use that categorical reader. New intended router code is not a faithful runtime replacement for every historical checkpoint.
