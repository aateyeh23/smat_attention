Joint ablation of trial22: restore scalar decay on distant transport and disable incidence output rescaling. Keep restored neighboring write gradients, detached hash input, content-only one write and four weighted reads, initialization seed123, original optimizer and strict baseline gates unchanged. Train from scratch; compare matched epochs to source-verified trial22 and GDN. This tests removing both changes together, not their separate effects.
