From trial07: add total correlation (KL of joint soft write occupancy against the product of its coordinate marginals) to existing marginal hash balance, using the existing .01 balance coefficient. Joint counts aggregate the training batch per head; beta only weights scalar counts and remains detached for the auxiliary loss. No additional payload writes or reads. This penalizes diagonal collapse missed by separate coordinate histograms while leaving the hard forward and task-gradient surrogate unchanged.

Rejected at epoch4:52.3584375% versus60.65875%. First-layer T64 hash-coordinate cosines at this checkpoint are .7675 and .5352, versus .9693 and .9913 at trial07 epoch6 (not an epoch-matched comparison). Less-parallel projection rows are therefore not sufficient evidence of improved retrieval. Next test removes operator transport while training tied causal memory features directly through retrieval.
