Selective copying: length 4096; 16 content tokens from vocabulary 16; width 64; 2 layers; batch 32; seed 0; 10000 steps; 500-step warmup; lr .001; AdamW wd .1; BF16; gradient clipping 1; evaluate and checkpoint every 250 steps on fixed 256 examples. Matches the Mamba-2 selective-copying data and training recipe. GDN uses the existing selective-copying baseline setting: head_dim 16, four heads, expand_v 2. The one/two-head restriction is specific to MQAR. Native GDN is d1; SMAT d2/d3/d4 restart local state at midpoint and use per-profile delta memory, sigmoid read gate, recurrent-output hashes with causal convolution, and 1000-step routing anneal. GDN and SMAT have identical underlying GDN dimensions. GDN recurrent output is projected to model width for hashing. Mamba-specific dt writes are replaced by GDN beta delta updates; no additional temporal memory decay.
